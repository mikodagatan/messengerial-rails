<div class="col-12 text-left">
  <div class="v-padding medium">
    <span class='small-index'>Address <%= @addresses.index(address) + 1 %></span>
    <%= address.first_line %> <%= address.second_line %> <%= address.city_town %>, <%= address.province %>, <%= address.country %> <%= address.zipcode%>
  </div>
</div>
