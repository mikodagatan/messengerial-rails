<span class="thick"><%= current_user.full_name %>:</span> Tasks From <%= better_date(@start_date) %> to <%= better_date(@end_date) %>

<br>

<%= render partial: 'task2', collection: @tasks, as: 'task' %>
